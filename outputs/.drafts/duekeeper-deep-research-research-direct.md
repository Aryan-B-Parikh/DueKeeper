# Research Notes — duekeeper-deep-research (direct search)

**Mode:** Direct (lead-owned) — 3 distinct query angles as required. `web_search`/`fetch_content`/`alpha_search` not visible in current tool set (`default.read`/`bash`/`edit`/`write` only); all evidence is local-file-backed via `grep`/`read`/`bash` in `D:/DueKeeper`. No `alpha_get_paper` or `.pdf` fetch (Step 3).

## Search Terms Used (exact)

1. **Definition / history** — `DueKeeper definition history` → `grep -rn "DueKeeper" README.md` + `grep -rn "DeadlineKeeper|self-contained|never miss"` + `read AUDIT.md §1-§2, README.md:3-5`
2. **Mechanism / formula** — `DueKeeper reminder engine mechanism formula` → `grep -rn "PLANNER_HORIZON|DUE_SOON_WINDOW|outboxClaimLimit|plannerGrace|notification_outbox|idx_outbox"` + `grep -rn "N = 16384|scryptSync|HKDF|WebPush: info|Content-Encoding: aes128gcm"` + `read lib/time.ts:1-2, lib/password.ts:3, lib/push/webpush.ts:22-62, db/schema.ts:71-89`
3. **Current usage / comparison (docs vs code + defaults/tests)** → `grep -rn "MAX_LIST_PAGE_SIZE|SSE_MAX|LOGIN_RATE|JWT_EXPIRES|ENCRYPTION_KEY|inTransaction|queryAll|isValidTimezone|snooze"` + `grep -rn "129 tests|33 suites"` + `bash npm test` + `read docs/API.md:18-35, docs/ARCHITECTURE.md, docs/DATABASE.md, config/env.ts:142-179`

## Notes — Definition / History

- **What DueKeeper is:** `README.md:3` “Never miss what's due. A full-stack deadline & reminder platform: track exams, submissions and hackathons, extract deadlines from messy text or screenshots, forward emails to your own inbox address, import/export calendars, and get reminders delivered through a crash-safe notification engine.” `README.md:5` “from-scratch, self-contained take on ‘DeadlineKeeper’ — but with zero external infrastructure”.
- **Scope of prior audit:** `AUDIT.md:1-3` `D:\DueKeeper\server` 53 TS files ~7k lines, 2026-08-24, stack Express 4 / TS strict / `node:sqlite` `DatabaseSync` / Zod / multer / nodemailer / Node ≥24, 5 runtime deps. Overall 6.8/10, single most important finding: “web push has never worked, and nothing in the system can tell you that.”
- **Paper evolution:** `AUDIT.md §9` dated 2026-08-25 claims every finding C1-C4, H1-H8, medium §5, §8 17-step plan is closed in working tree, with file+mechanism per row and residual risks accepted rather than fixed. Scores in §7 not restated (still describe 2026-08-24 tree).
- **Source locations:** `README.md`, `AUDIT.md`, `docs/ARCHITECTURE.md` system overview diagram, `docs/DATABASE.md` table ledger, `docs/API.md` contract.

## Notes — Mechanism / Formula

- **Time windows:** `server/src/lib/time.ts:1-2` `DUE_SOON_WINDOW_MS = 72*60*60*1000` (72h), `PLANNER_HORIZON_MS = 7*24*60*60*1000` (7d). `server/src/engine/planner.ts:99,164` uses `now + DUE_SOON_WINDOW_MS` for status recompute and `now + PLANNER_HORIZON_MS` for horizonEnd.
- **Outbox claim (C2 fix):** `engine/outbox.ts:96-104` `SELECT id FROM notification_outbox WHERE status='pending' AND scheduled_at<=? AND (next_retry_at IS NULL OR next_retry_at<=?) ORDER BY COALESCE(next_retry_at,scheduled_at) LIMIT ?` with `config.outboxClaimLimit 50` (`config/env.ts:165` `intInRange 1-500, default 50`). Backoff written in `scheduleRetry` as `min(600_000, 30_000*2^(attempts-1))` (`engine/outbox.ts:251`). Old index `idx_outbox_claim(status,scheduled_at,next_retry_at)` now `idx_outbox_ready(status,next_retry_at,scheduled_at)` (`db/schema.ts:231`). Reclaims separate: `notification_outbox.reclaims` (`schema.ts:227`) + `config.outboxMaxReclaims 3` (`env.ts:167`).
- **Planner grace (C3/Medium):** `plannerGraceSeconds 60` (`config:169` `0-3600`) in `planner.ts:177` `datetime('now', printf('-%d seconds',?))` and `scheduling.ts:50` `at >= now - grace*1000 && at <= now + HORIZON`. `UNIQUE(reminder_id,scheduled_for)` (`schema.ts:162-178` migration `006` rebuild) replaces old `UNIQUE(reminder_id)`.
- **Crypto:** `lib/password.ts:3` `N=16384, r=8, p=1`, 16B salt, `NFKC`, `scrypt$N$r$p$salt$key`, `timingSafeEqual`. `lib/push/webpush.ts:28-53` HKDF wrapper notes `ikm` before `salt` (Node order), `deriveContentKeys` `key_info="WebPush: info\0"‖ua‖as`, `ikm=HKDF(auth,ecdh,key_info,32)`, `cek=HKDF(salt,ikm,"Content-Encoding: aes128gcm\0",16)`, `nonce` similarly. `tests/push.test.ts:20-21` swap omission note.
- **DB transactional primitive:** `db/database.ts:28-33` `queryAll<T>/queryOne<T>` helper (this loop) + `inTransaction<T>` (`:50-93`) with `BEGIN IMMEDIATE`/SAVEPOINT nesting, used in `outbox.ts:91,218`, `planner.ts:205`, `scheduling.ts:68,156,210`, `calendar.routes.ts`, `events.service.ts`.

## Notes — Current Usage / Comparison (docs vs code + tests)

- **Auth defaults:** `config/env.ts:142` `jwtExpiresIn 15m`, `:144` `loginRateLimit 10/15m`, `:145` `registerRateLimit 30/60m`, `:176` `sseMaxConnections 5`, `:179` `maxListPageSize 100` (10-1000). `AUDIT.md §6` and `README` “15-min access + rotating refresh with theft detection” match `lib/tokens.ts:148-180` (`revokeAllSessions` bumps `token_version` + `revoked_at` in `inTransaction`) + `modules/users/users.routes.ts:124-157`. Rate-limit fix: `req.ip` via `trust proxy` (`app.ts:22`), `lib/rateLimit.ts:22-55` `maxKeys 10000`, `ipLoginLimiter 30` + per-email `loginLimiter` + `refresh/logout/password` limiters.
- **Validation:** `lib/datetimeValidation.ts:42-95` `OFFSET_DATETIME` regex mandates offset/Z, `isValidTimezone` requires `/`, `validateInstant` rejects `2026-02-31` via `isValidCivilDate`. `lib/zonedTime.ts:60-145` two-pass `zonedToUtc` + `civilDateInZone`. `modules/events/events.routes.ts` `snooze` bounded `^(\d{1,7})([mhd])$` `1m…30d` + `±8.64e15`. HTTP test `http.test.ts:214-255` naive→422, fixed-offset→422, `+05:30`→201.
- **Pagination:** `API.md:24-28` now `limit` defaults 50, clamped to 100, malformed→422. Code: `lib/pagination.ts:18-55` `parsePageRequest` + `events.service.ts:155-185` + `notifications.routes.ts:88-210` filter in SQL (`statusPredicate` + `DUE_SOON_WINDOW_MS`). Prior `LIMIT 500` + post-filter replaced.
- **SSE:** `ARCHITECTURE.md` ticket flow; code `notifications.routes.ts:88-210` `POST /stream-ticket` 30s single-use + `GET /stream?ticket` or `Bearer` via `authenticateBearer` (checks `token_version`), rejects `?token=<jwt>` (401), backpressure `!res.write→end`, cap 5, `closeNotificationStreams` for shutdown. `server/src/index.ts:55-85` drain `outboxBusy` poll (now `while` loop), `unhandledRejection`/`uncaughtException` → `metrics.unhandledErrors`.
- **Secrets/operability:** `lib/secretbox.ts:12-47` `ENCRYPTION_KEY` base64 32B (now reports `got N bytes`), `isProd` throws if missing, `PREVIOUS_ENCRYPTION_KEYS` rotation (prod fail-fast in `config/env.ts:194-207`), `v1.iv.blob` tries `getAllKeys()`. `engine/channels/emailChannel.ts` SMTP `host:port` + `connectionTimeout/greetingTimeout/socketTimeout 10s`, `sendRaw` sets `Message-ID`/`X-Entity-Ref-ID` from `idempotencyKey`. `config:182-235` prod guards: JWT≥32, `ENCRYPTION_KEY` charset+length, `!=JWT_SECRET`, `APP_BASE_URL https`, `CORS localhost`, `SMTP_HOST`.
- **Tests:** `server/package.json` test `tsx src/tests/index.test.ts`; `README.md:49-50` originally 49 unit tests vs current `npm test 129 tests / 33 suites / 0 fail` (vector + http integration). `tests/push.test.ts:14-116` RFC 8291 §5 vector (ikm/cek/nonce/body + swap/reversed negative tests). `http.test.ts:176-990` covers C4/H1-H8/backoff/lease/pagination. Prior 2 failures (GOOGLE env leak) fixed this loop (correct `GOOGLE_REDIRECT_URI` + isolation).
- **Remaining disagreements/uncertainties:** `README` perf `~6.8k/2.6k p99<30ms` unverified (no bench run, `web_search` not exposed); live-browser push not exercised beyond vector; inbox HMAC still not present (`AUDIT.md §9` accepted); single-instance SQLite (WAL/FK/busy_timeout) vs horizontal scale Postgres path (`AUDIT.md §9`, `DATABASE.md`).
